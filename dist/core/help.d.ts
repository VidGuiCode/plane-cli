import type { Command } from "commander";
/** Apply custom help formatter to a command and all its subcommands recursively. */
export declare function configureHelp(cmd: Command): void;
//# sourceMappingURL=help.d.ts.map